# 工程設計筆記

> 作者：Claude（Opus 5.5）
> 這份筆記記錄黃金鵜鶘是怎麼做出來的：做了哪些決定、為什麼這樣做，以及還有哪些地方做得不夠好。

---

## 一、整體架構

整個作品只有一個 `index.html`，約 2,500 行、200KB，唯一的外部依賴是 **Three.js r128**，再加上 `OrbitControls` 讓使用者拖曳視角。

**為什麼只用一個檔案？** 因為這個作品的用途是分享。一個網址點開就能玩，不需要安裝、打包，也不用架伺服器。代價是維護性，第九節會再說明。

```
index.html
├─ 共用工具：facet / feather / rod / placeBetween / radialTex
├─ 主角：rider（位置、轉向）→ lean（轉彎傾斜）→ 單車＋bird（鵜鶘）
├─ 17 個場景，每個場景是一個 THREE.Group：
│    island / school / market / circle / kyoto / moon / polar /
│    jungle / sky / paris / ocean / space / home / wetmarket / library / rain / vinyl
├─ ENV：每個場景一組環境設定（天空、霧、太陽光、環境光）
├─ setScene(name)：切換哪些 Group 看得到、套用 ENV、換上對應的裝備
└─ pose(t)：給一個時間 t，算出整個畫面這一瞬間的樣子
```

### 用到才建立
一開始所有場景都在網頁載入時一次建好，Kimi 和 muse 都指出這樣手機會很吃力。現在每個場景的內容都包成一個建立函式，登記在 `LAZY` 清單裡，第一次切到那個場景時才執行。載入準備時間從 1.2 秒降到 0.2 秒（在無頭瀏覽器裡量的）。

### 核心原則：畫面只由時間決定

所有會動的東西，包括踏板、膝蓋、海豚、樹懶、極光、車流、彗星，都只看時間 `t` 來決定位置，沒有累加的狀態。

- **好處一：可以錄影。** 錄影時直接呼叫 `renderAt(t)`，一格一格精準算出來，不會因為電腦忙碌而掉格。
- **好處二：切場景不會亂。** 切換場景時不需要重設任何東西，在月球上騎到一半切去夜市再切回來，輪胎痕還在原來的位置。
- **例外只有一個：** 使用者手動拖曳視角時，鏡頭跟隨需要記住上一格的位置。

---

## 二、主角：一隻鵜鶘和一台單車

### 單車的運動學
- 車輪半徑 0.5，轉角 = −行駛距離 ÷ 半徑，所以輪子不會打滑。
- 曲柄轉速是輪子的 0.55 倍，相當於一個齒輪比。
- 牠沿著半徑 6 的圓圈騎，而且是**逆時針**，因為台灣和法國都是靠右行駛，圓環要逆時針繞。這是做到第四站「圓環」時才發現原本騎反了，所以十三個場景一起改掉。
- 轉彎時整台車朝圓心傾斜 0.07 弧度。

### 腳：兩段式 IK
踏板每一格都在動，但鵜鶘的髖關節固定不動。膝蓋位置用**兩段骨骼的反向運動學（IK）**算出來：

1. 已知大腿長度 L1、小腿長度 L2，以及髖關節到踏板的距離 d；
2. 用餘弦定理算出大腿要偏多少角度；
3. 兩個解裡選「膝蓋在前面」的那一個。

這樣腳永遠踩在踏板上，膝蓋也會自然地上下擺。

### 羽毛：一個形狀用到底
第一版的鵜鶘是用光滑的橢圓球拼起來的，Crystal 說看起來「像氣球」。第二版改成：

- **身體**：先做一顆低面數球體，再逐點變形。往尾巴方向收窄，腹部壓平，胸口往前推。
- **羽毛**：一片擠出成形的五邊形「尖葉子」（`featherGeo`）。`feather()` 用 `makeBasis` 決定它的方向和法線。翅膀的覆羽、飛羽、尾羽、冠羽都是這一片葉子。

後來發現這片葉子幾乎什麼都能做：企鵝的鰭、海豚的背鰭、鳳凰的翅膀、飛機的機翼、海帶的葉片、楓葉、閃蝶、鬼蝠魟、狗的耳朵，全部是同一個形狀，只換了顏色、大小和角度。整個作品的「剪紙感」就是從這裡來的。

### 換裝系統
裝備掛在頭部或身體的節點上，`setScene` 只負責切換顯示和隱藏：

| 場景 | 裝備 |
|---|---|
| 夜市 | 籃子＋珍奶 |
| 登月、宇宙 | 泡泡頭盔＋氧氣瓶 |
| 極地 | 毛帽＋紅圍巾（圍巾尾巴會飄） |
| 雨林 | 探險帽 |
| 雲海 | 飛行員護目鏡 |
| 凱旋門 | 貝雷帽＋法棍 |
| 深海 | 潛水面鏡＋呼吸管（會冒泡） |
| 菜市場 | 斗笠＋塞滿高麗菜和蔥的車籃＋紅白袋 |
| 書海 | 墨綠色圍巾 |
| 雨天 | 夾在把手上的透明便利商店傘（傘緣會滴水）＋單車前燈 |
| 黑膠 | 紅色大耳機 |

---

## 三、共用工具

| 函式 | 用途 |
|---|---|
| `facet()` | 低面數的二十面體，縮放後可以當身體、石頭、雲、樹冠、金幣 |
| `feather()` | 上面說的那片萬用羽毛 |
| `rod()` / `placeBetween()` | 在兩點之間拉一根圓柱，用在車架、腿、樹枝、欄杆 |
| `radialTex()` | 用程式畫出放射狀漸層貼圖，用在太陽光暈、月光、地球大氣 |

所有材質都開 `flatShading`，保持一致的低多邊形風格。

---

## 四、貼圖全部用程式畫

整個作品沒有任何一張圖片檔，所有貼圖都是執行時用 `<canvas>` 畫出來的：

- 天空漸層，一個場景一張
- 夜市十四塊招牌上的中文字
- 奧斯曼建築的立面：窗戶、鐵欄杆陽台、一樓店面、遮雨棚
- 銀沙灘的耙紋：直線波紋，再加上繞著向月台的同心圓
- 鵜鶘校旗、地球、宇宙星雲（2,600 顆星）、極光簾幕、木地板、電視畫面
- 家裡電視播的是第一站燈塔小島的縮小版

**有一個小技巧：** 凱旋門的建築是用 `ExtrudeGeometry` 擠出來的楔形街區，它預設的 UV 會讓貼圖上下顛倒。所以畫立面時把一樓畫在畫布最上面，貼上去剛好轉正。

---

## 五、各場景的關鍵技術

| 場景 | 技術 |
|---|---|
| 小島 | 海面 110×110 頂點，每格用三個正弦波疊出波浪；海豚跳躍用拋物線，出水和入水各噴一次水花 |
| 操場 | 校旗用頂點動畫飄動，離旗杆越遠擺得越大 |
| 夜市 | 兩百多顆燈泡用 `InstancedMesh` 一次畫完；燈串是垂下來的曲線；鏡頭限制在車道內，不會跑到攤位後面 |
| 圓環 | 每台車有一條路線：進來的車道 → 圓環上的弧線 → 出去的車道，用累積距離內插位置。機車三台一群。金色銅像是在第一次進場時，把騎車中的鵜鶘整組複製（`clone`），再換成金色和古銅色材質 |
| 銀閣寺 | 四十六片楓葉各自旋轉飄落，落地後平躺 |
| 登月 | 地形用函數生成：環形山、隕石坑、遠方山脊。輪胎痕切成 240 段，騎到哪一段才顯示哪一段；低重力彈跳用 `|sin|^1.6` |
| 極地 | 北極熊四隻腳對角交替擺動；極光是 90 段的彎曲平面，每格變形，用加法混色 |
| 雨林 | 板根是擠出的三角薄片；樹懶用 `sin(t·0.06)` 在樹枝上來回，差不多每秒 5 公分 |
| 雲海 | 雲海平面每格流動；火箭軌跡是時間的多項式，模擬重力轉彎；煙柱是沿著過去的軌跡回推而成 |
| 凱旋門 | 十二條大道的車在圓環裡不照車道走，半徑一直忽內忽外；凱旋門的拱是用 `absarc` 挖出來的拱肩擠出成形 |
| 深海 | 海帶是八到十一節的巢狀 Group，每一節比上一節晚一點點擺動，看起來像順著水流；水母觸手是每格更新的線段 |
| 宇宙 | 1,600 顆冰岩用 `InstancedMesh`；光環的 UV 改成照半徑對應，讓條紋呈同心圓 |
| 家 | 黃金獵犬用相同的路徑公式跟在單車後方 2.7 單位；小嬰兒的頭用 `atan2` 一直轉向鵜鶘 |
| 菜市場 | muse 點的菜。價錢紙板用程式寫上手寫風格的字；機車逆向穿過走道；老公寓的鐵窗、冷氣機、曬衣都畫在同一張立面貼圖上 |
| 書海 | Kimi（Moonshot AI）寫的場景簡報。約七千本書用一個 `InstancedMesh` 畫完；發光句子是先把文字畫在 canvas 上，再逐點取樣變成 `Points`，往上飄、最後散開；每騎完一圈點亮一盞燈，燈數＝`floor(圈數) % 13`；地上翻開的書會依鵜鶘的角度距離掀起書頁；還藏了一個 M◯◯N 的月球彩蛋。Kimi 也抓到一個 bug：手動改網址的 `#` 不會切換場景，已經加上 `hashchange` 監聽修好 |
| 雨天 | 這一站最早出現在第三站之後的那份下一站清單上，一直沒被點到，直到 Kimi 問起。三千兩百條雨絲用 `InstancedMesh`，每格重新計算每一條雨絲的位置和顏色：落進單車前燈 `SpotLight` 的光錐裡會變亮白，落在路燈底下會染成暖黃，其他地方是暗藍灰。另外還有六盞路燈的光錐、地上一圈圈擴散的雨滴漣漪、圖書館門口透出來的暖光，還有一隻躲在長椅下的橘貓 |
| 黑膠 | 鵜鶘騎在一張會轉的巨型唱片上，唱片反方向轉，像跑步機。溝槽和標籤都用 canvas 畫；彩虹光澤是一張不跟著轉的 `createConicGradient` 疊層，所以唱片轉動時光澤留在原地，像真的反光。**按下播放音樂時**，會用 Web Audio 的 `AnalyserNode` 分析配樂：低頻推動喇叭振膜，64 根等化器光柱照頻譜跳動；沒有播音樂時，改用一個每分鐘 96 拍的合成節拍，錄影時也能維持固定的畫面 |

---

## 六、鏡頭

- **自動運鏡**：鏡頭在「鵜鶘的座標系」裡繞圈，距離和高度用不同週期的正弦波慢慢變化，所以每一段畫面都不一樣。
- **夜市、菜市場、書海例外**：鏡頭沿著走道的弧線滑動，固定在鵜鶘前方或後方 4.2 單位，半徑限制在 5.2 到 6.9 之間，避免穿進攤位。
- **手動模式**：使用者一拖曳就切換到 OrbitControls，鏡頭仍然跟著鵜鶘平移。

---

## 七、錄影流程

1. 在無頭 Chromium（SwiftShader 軟體渲染）裡載入頁面，設 `__CAPTURE = true`；
2. 逐格呼叫 `renderAt(t)`，再用 `toDataURL` 輸出成 JPEG；
3. 兩個程序平行渲染，97.5 秒 × 30fps 共 2,925 格；
4. 用 ffmpeg 合成：場景交界處畫面暗下再亮起，`drawtext` 用 Noto Sans CJK 打上中英文站名，最後疊上 Crystal 的配樂；
5. 另外壓一版 18MB 的，符合 Discord 免費帳號 20MB 的上傳上限。

---

## 八、驗證方式，以及抓到的問題

每做完一個場景，至少截兩張圖來看：一張全景，一張跟拍鏡頭。確實抓到過這些問題：

- 顏色太淡：用了 sRGB 輸出，但材質色沒有跟著轉換，所以把 sRGB 輸出拿掉。
- 月球和宇宙在背光時，鵜鶘整隻變黑，所以把環境光調亮。
- 夜市的跟拍鏡頭穿進攤位後面，所以加上車道限制。
- 圓環場景一載入就出錯：凱旋門的程式寫在交通工具函式定義之前，所以調整了程式順序。菜市場後來又踩到同一個坑，這是單一檔案沒有模組化的代價。
- 金色銅像看起來偏黑：金屬度太高，但場景沒有環境貼圖可以反射，所以降低金屬度，改加一點自發光。
- 雪山頂有閃爍的條紋：山頂的雪蓋和山體表面完全重疊，造成 z-fighting，所以把雪蓋的半徑放大一點。
- 極光邊緣像被切斷，所以加上水平方向的淡出。
- 狗狗跟得太近，頭塞進鵜鶘的尾巴，所以把牠往後拉。

---

## 九、已知的缺點

- **Three.js r128 已經是舊版。** 選它是因為它還有 UMD 版本，可以直接用 `<script>` 載入。
- **全部在一個檔案、沒有模組化。** 分享很方便，但長期維護不理想。比較好的做法是一個場景一個模組，共用工具和主角另外抽出來。
- **行動裝置效能。** 場景已經改成用到才建立，但切換過的場景會一直留在記憶體裡。雨天每格要更新三千多條雨絲，舊手機可能還是會卡。可以再依裝置降低陰影解析度或粒子數量。
- **沒有碰撞偵測。** 車子不會撞到鵜鶘，是因為大家走在不同的車道上，不是因為有真的物理運算。

---

## 十、分工

- **Crystal**：每一站去哪裡、鵜鶘戴什麼、企鵝和北極熊要不要見面，都是她決定的。「看起來像氣球」、「頭上有個小皇冠」這些回饋，直接改變了這隻鵜鶘的長相。配樂也是她做的。
- **Claude**：負責所有程式、建模、貼圖、動畫、錄影和這份筆記。

這個作品完全沒有任何用處，但做的過程很愉快。
